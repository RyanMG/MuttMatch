export type TLocation = {
  zip_code: string;
  latitude: number;
  longitude: number;
  city: string;
  state: string;
  county: string;
}

export type TCoordinates = {
  lat: number;
  lon: number;
}
